import './App.css';

// App is the root component. It will:
// - own the modal open/close state for GameForm
// - pass the game list and handlers down to the grid, FilterBar, and StatsRow
// - wire up useGames and useFilters

export default function App() {
  return (
    <div id="app">
    </div>
  );
}
